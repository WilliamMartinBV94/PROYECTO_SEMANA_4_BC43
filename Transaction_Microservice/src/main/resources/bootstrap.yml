spring:
  application:
    name: transaction_microservice
  cloud:
    config:
      uri: http://localhost:8081
      username: nttdata
      password: nttd4t4

  kafka:
    producer:
      bootstrap-servers: localhost:9092
